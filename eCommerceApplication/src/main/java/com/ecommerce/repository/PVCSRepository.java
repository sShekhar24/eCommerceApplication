package com.ecommerce.repository;

import com.ecommerce.entity.PVCS;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PVCSRepository extends JpaRepository<PVCS, Long> {
}
